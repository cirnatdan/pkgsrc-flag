This is a repository of my custom pkgsrc packages. Some of them might already be in pkgsrc-wip.
Packages are not guaranteed to compile.

INSTALL:
	cd /urs/pkgsrc
	git clone git://github.com/plextoria/pkgsrc-flag.git flag

UPDATE:
	cd /usr/pkgsrc
	git pull
